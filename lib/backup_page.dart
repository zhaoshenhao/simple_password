import 'package:flutter/material.dart';

class FilesAndBackupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        title: Text("Files & Backup"),
      ),
      body: new Center(
        child: new Text("Files and Backup page"),
      ),
    );
  }
}
